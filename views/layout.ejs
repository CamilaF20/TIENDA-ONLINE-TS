<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Tienda Online</a>
      <div>
        <% if (locals.user) { %>
          <span class="text-white me-3">Hola, <%= locals.user.email %></span>
          <a class="btn btn-outline-light me-2" href="/api/auth/logout">Cerrar sesión</a>
          <% if (locals.user.role === "admin") { %>
            <a class="btn btn-success" href="/admin">Panel Admin</a>
          <% } else { %>
            <a class="btn btn-primary" href="/client">Mi cuenta</a>
          <% } %>
        <% } else { %>
          <a class="btn btn-outline-light me-2" href="/login">Iniciar sesión</a>
          <a class="btn btn-success" href="/register">Registrarse</a>
        <% } %>
      </div>
    </div>
  </nav>

  <main class="container py-4">
    <%- body %>
  </main>
</body>
</html>
